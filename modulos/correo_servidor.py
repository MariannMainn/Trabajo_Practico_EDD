#clase servidorCorreo
class servidorCorreo:
        def __init__(self):
        self.user = [] 
        self.mensajes = []
    #Metodos 
        def registrarUser(self, user):
        self._users.append(users)

        def enviarMensaje(self, destinatario):
        self._buscarUser(destinatario)
        
        def _buscarUser(self, correo):
        self._users
